# 문자열 - 알파벳 ↔️ Bit

전제 하나만 먼저
- 알파벳 → 숫자
- A = 0
- B = 1
- C = 2
- D = 3

비트는 오른쪽이 0번 비트

## 1️⃣ 알파벳을 비트로 만들기 `1 << alphabet`

### 예: C를 방문했다고 표시하고 싶다
```
alphabet = 2; // C
bit = 1 << 2;
```
비트로 보면: `0000 0100` → “C를 방문했다”는 표시

### 예: A, C를 둘 다 방문했다면?
```
visited = (1 << 0) | (1 << 2); // 👉 visited = 5

A: 0000 0001
C: 0000 0100
---------------- OR
0000 0101
```
---
## 2️⃣ 방문 여부 확인: AND연산 `visited & bit`
### 예: 지금 `visited = 0000 0101 (A, C 방문)` 상태
### ❓ C를 이미 방문했는지?
```
bit = 1 << 2; // C
if(visited & bit != 0) : true -> 이미 방문한 것 

visited: 0000 0101
bit:     0000 0100
---------------- &
0000 0100  (≠ 0) ✅ 이미 방문함
```


### ❓ B를 방문했는지?
```
bit = 1 << 1; // B
if(vistied & bit != 0) : false -> 아직 방문 안한 거  

visited: 0000 0101
bit:     0000 0010
---------------- &
0000 0000
```

---

## 3️⃣ 방문 처리: OR 연산 `visited | bit`
### 예: 현재 A, C 방문 상태에서 → B를 새로 방문
```
visited = visited | (1 << 1);

visited: 0000 0101
B bit:   0000 0010
---------------- OR
0000 0111 👉 이제 A, B, C 방문
```
---
## 4️⃣ 방문 해제 (백트래킹, ^ XOR)
### 왜 필요?
- DFS에서 갔다가 돌아올 때 “방문 기록을 원래대로” 돌려야 함

### 예: 현재 A, B, C 방문 (0000 0111)
→ B를 해제하고 싶다
```
// visited XOR (B를 비트로 표현한 거)
visited = visited ^ (1 << 1);

visited: 0000 0111
B bit:   0000 0010
---------------- ^
0000 0101 👉 다시 A, C만 방문
```
---
## 5️⃣ 한 줄로 흐름 정리 (DFS에서 실제 쓰는 패턴)
```
int bit = 1 << map[nr][nc];

if ((visited & bit) != 0) continue; // 이미 방문

dfs(nr, nc, visited | bit); // 방문하고 이동

// 복귀하면 자동으로 원래 visited 유지됨 (값 복사)
```

> [!note]
> 👉 중요한 사실
> visited는 int라서 값 복사 
> 굳이 XOR로 되돌릴 필요도 없음 (지역 변수라서)